{
  "id": "skilzvolt",
  "name": "SkilzVolt",
  "description": "Owner-only, fixed-endpoint access to governed SkilzVolt workspace skills.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "connectionKeyEnv": {
        "type": "string",
        "default": "SKILZVOLT_CONNECTION_KEY",
        "description": "Legacy fallback: environment variable containing a static SkilzVolt bearer connection key, used only when no `openclaw skilzvolt login` OAuth session is connected."
      },
      "allowProposals": {
        "type": "boolean",
        "default": true,
        "description": "Allow the three governed SkilzVolt proposal operations."
      },
      "crmSharePointEnabled": {
        "type": "boolean",
        "default": false,
        "description": "Explicitly enable the fixed owner-only CRM/SharePoint handoff after the workspace CRM worker and service-owned queue producer have been installed. Enabling SkilzVolt alone never enables this route."
      },
      "expenseSharePointEnabled": {
        "type": "boolean",
        "default": false,
        "description": "Explicitly enable the fixed owner-only expense SharePoint boundary after the Pi finance service, queue processor, and media permissions have been installed. Enabling SkilzVolt alone never enables this route."
      },
      "agentIds": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": ["main"],
        "description": "Agents that receive SkilzVolt organisation-knowledge guidance."
      },
      "organisationSkillNames": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Local managed skills eligible for verified migration retirement."
      }
    }
  }
}
