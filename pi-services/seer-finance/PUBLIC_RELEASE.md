# Public-release boundary

The parent repository is public. This import contains source code, synthetic tests and a synthetic example fixture only.

Excluded deliberately: all SQLite files, `transactions.json`, bank statements, receipts, attachments, runtime queues/state, backup/restore outputs, reconciliations, credentials, tokens, API keys, `.env` files, company profile data and Pi-specific operational runbooks.

No installer, timer, migration, database operation or production runtime is enabled or changed by this source import.
