[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "l1-voice-telephony"
version = "0.1.0"
description = "L1 telephone voice gateway — local no-call foundation"
requires-python = ">=3.11"

